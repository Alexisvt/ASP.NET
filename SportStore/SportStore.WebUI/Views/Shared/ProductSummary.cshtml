@model SportStore.Domain.Entities.Product
<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <strong class="pull-right label label-primary">@Model.Price.ToString("c")</strong>
    </h3>
    <span>@Model.Description</span>
</div>

